{
  "name": "memory-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:node": "node --experimental-vm-modules node_modules/.bin/vitest"
  },
  "dependencies": {
    "@durable-streams/core": "workspace:*",
    "@durable-streams/storage-memory": "workspace:*",
    "@durable-streams/server-conformance-tests": "^0.1.4",
    "vitest": "^4.0.16"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
